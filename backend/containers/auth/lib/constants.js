const {constants} = require('../../../common/lib/index');

const {
  USERS,
  getResponse,
  ERRORS,
  ERROR_MSG,
  REDISFORMAT,
} = constants;
module.exports = {
  USERS,
  getResponse,
  ERRORS,
  ERROR_MSG,
  REDISFORMAT,
};
